start:
  - run: ./.backend/start.sh
stop:
  - run: ./.backend/stop.sh

auto_start: true

env:
  - DEBUG=false

stdout_log: debug
stderr_log: warn

http_proxy:
  port: 8000
  strip_prefix: /web/apps/neo-blackbox/